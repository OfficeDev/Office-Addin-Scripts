{
  "name": "customfunctionsplugin",
  "version": "0.0.1",
  "description": "Plugin to auto generate custom functions metadata",
  "main": "./lib/customfunctionsplugin.js",
  "scripts": {
    "tsc": "tsc -p tsconfig.json",
    "tsc-watch": "tsc -p tsconfig.json -w",
    "build": "rimraf lib && concurrently \"npm run tsc\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Office Dev",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.0.1",
    "es6-promise": "^4.2.5",
    "rimraf": "^2.6.2",
    "typescript": "^3.1.6",
    "webpack": "^4.25.1"
  },
  "devDependencies": {
    "@types/node": "^10.12.12"
  }
}
