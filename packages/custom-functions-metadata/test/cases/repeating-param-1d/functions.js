/**
 * custom function with 1d repeating parameter
 * @customfunction
 * @param {number} first
 * @param {number[]} add
 */
function repeat1d(first, add) {
    // empty
}

/**
 * custom function with only repeating parameter
 * @customfunction
 * @param {number[]} add
 */
function repeat1dOnly(add) {
    // empty
}