steps:
- pwsh: |
    $env:NPM_CONFIG_PROGRESS = "false"
    $env:NPM_CONFIG_AUDIT = "false"
    $env:NPM_CONFIG_FUND = "false"
    $env:CI = "true"
    npm ci --no-audit --no-fund --progress=false 2>&1 | ForEach-Object { "$_" }
  displayName: "Install NPM packages"
  timeoutInMinutes: 5