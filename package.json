{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "npm-dist-tags": "lerna exec npm dist-tag ls --stream",
    "postinstall": "lerna bootstrap",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.14.1"
  }
}
